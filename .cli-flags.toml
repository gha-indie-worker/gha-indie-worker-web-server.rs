# flags-2-env surface for `ghaiw-web`
#
# Every flag declares canonical `aliases` (flags2env `audit_config` rejects a
# flag with no long alias) and every env destination appears exactly once
# (duplicate env targets are an audit error).

[help]
url = "https://github.com/gha-indie-worker/gha-indie-worker-web-server.rs"

[parse]
allow_unknown = false

[flags.config]
env = "GHA_INDIE_WORKER_CONFIG"
aliases = ["config"]
type = "string"
help = "Path to TOML configuration"

[flags.api_base]
env = "GHA_INDIE_WORKER_API_BASE"
aliases = ["api-base"]
type = "string"
help = "API HTTP base URL"

[flags.json]
env = "GHA_INDIE_WORKER_JSON"
aliases = ["json"]
type = "bool"
default = "false"
help = "Emit JSON"

[flags.gha-indie-worker-api-http-base]
env = "GHA_INDIE_WORKER_API_HTTP_BASE"
aliases = ["gha-indie-worker-api-http-base", "api-http-base"]
type = "string"
help = "API HTTP base URL."

[flags.gha-indie-worker-database-url]
env = "GHA_INDIE_WORKER_DATABASE_URL"
aliases = ["gha-indie-worker-database-url", "database-url"]
type = "string"
help = "Optional database URL for direct reads."

[flags.gha-indie-worker-web-bind]
env = "GHA_INDIE_WORKER_WEB_BIND"
aliases = ["gha-indie-worker-web-bind", "bind"]
type = "string"
default = "127.0.0.1:8081"
help = "HTTP bind address."
