name: Web flags and repository governance

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rust-and-config:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Check out web candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
        with:
          toolchain: '1.90.0'
          components: rustfmt, clippy

      - name: Verify committed lock
        run: cargo metadata --locked --format-version 1 >/dev/null

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Test without optional transports
        run: cargo test --locked --no-default-features

      - name: Test all features
        run: cargo test --locked --all-features

      - name: Run strict Clippy
        run: cargo clippy --all-targets --all-features --locked -- -D warnings

      - name: Verify secret-capable connection URLs are not public flags
        run: |
          set -euo pipefail
          for table in \
            '[flags.gha-indie-worker-api-http-base]' \
            '[flags.gha-indie-worker-database-url]'; do
            if grep -Fq "$table" .cli-flags.toml; then
              echo "secret-capable connection URL is public: $table" >&2
              exit 1
            fi
          done
